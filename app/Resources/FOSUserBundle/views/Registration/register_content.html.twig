{% trans_default_domain 'FOSUserBundle' %}
        {{ form_start(form) }}
        {{ form_widget(form) }}

        <div>
            <input type="submit" class="btn waves-effect waves-light col s12" value="{{ 'registration.submit'|trans }}" />
        </div>
        {{ form_end(form) }}




</form>
